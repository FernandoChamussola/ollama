version: "3.9"

services:
  ai-service:
    build: .
    container_name: ai-log-service-dev
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - OLLAMA_HOST=0.0.0.0:11434
    volumes:
      - ollama_models_dev:/root/.ollama

volumes:
  ollama_models_dev: